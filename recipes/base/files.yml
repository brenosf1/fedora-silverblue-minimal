modules:  
  - type: files
    files:
      - source: system
        destination: / # copies files/system/* (* means everything inside it) into your image's root folder /

      - source: monitors.xml
        destination: /etc/skel/.config/monitors.xml

      - source: icons/Adwaita-colors/
        destination: /usr/share/icons

      - source: backgrounds/default.png
        destination: /usr/share/backgrounds/default.png

      - source: config/gtk-3.0
        destination: /etc/skel/.config/gtk-3.0

      - source: config/gtk-4.0
        destination: /etc/skel/.config/gtk-4.0

