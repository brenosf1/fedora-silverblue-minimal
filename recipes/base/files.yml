modules:  
  - type: files
    files:
      - source: system
        destination: / # copies files/system/* (* means everything inside it) into your image's root folder /

      - source: monitors.xml
        destination: /etc/skel/.config/monitors.xml

      - source: icons/Adwaita-colors/*
        destination: /usr/share/icons
